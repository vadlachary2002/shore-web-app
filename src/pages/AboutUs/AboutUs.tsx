import React from 'react';
import { ErrorBoundary } from '../../components';

const AboutUs = () => {
    return (
      <ErrorBoundary>
      <h1>about us</h1>
      </ErrorBoundary>
    );
};

export default AboutUs;
