import React from 'react';
import { ErrorBoundary } from '../../components';

const ContactUs = () => {
    return (
      <ErrorBoundary>
      <h1>contact us</h1>
      </ErrorBoundary>
    );
};

export default ContactUs;
