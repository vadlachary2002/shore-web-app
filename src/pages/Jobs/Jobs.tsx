import React from 'react';
import { ErrorBoundary } from '../../components';

const Jobs = () => {
    return (
      <ErrorBoundary>
      <h1>jobs</h1>
      </ErrorBoundary>
    );
};

export default Jobs;
